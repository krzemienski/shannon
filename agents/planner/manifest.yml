# Shannon agent manifest — planner
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: planner
description: "Plan synthesis: intake → research → design → tasks → executable plan"
priority: P0

embedded_skills:
  - plan-author
  - interview-framework
  - goal-condition-architect
  - spec-workflow
  - create-meta-prompts
  - codebase-analysis
  - skill-inventory
  - library-docs-fetch

related_standalone_skills:
  - gepetto
  - interview-framework
