# Shannon agent manifest — meta-judge
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: meta-judge
description: "Pillar 4: rubric YAML generator before any judge/oracle/critic runs"
priority: P0

embedded_skills:
  - judge
  - consensus-engine

related_standalone_skills:
  - prompt-engineering-patterns
  - create-meta-prompts
