# Shannon agent manifest — executor
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: executor
description: "Generic task executor for plan steps — the most-spawned agent type"
priority: P0

embedded_skills:
  - dispatch-parallel
  - functional-validation
  - reflect
  - codebase-analysis

related_standalone_skills:
  - memorize
  - session-handoff
