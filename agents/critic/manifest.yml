# Shannon agent manifest — critic
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: critic
description: "Severity-rated review producing BLOCKING/HIGH/MEDIUM/LOW verdicts"
priority: P1

embedded_skills:
  - judge
  - evidence-gate

related_standalone_skills:
  - root-cause-tracing
  - consensus-engine
