# Shannon agent manifest — validator
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: validator
description: "Pillar 3: single-claim functional validation worker producing evidence + verdict"
priority: P0

embedded_skills:
  - functional-validation
  - evidence-gate
  - evidence-indexing
  - refusal-discipline

related_standalone_skills:
  - visual-inspection
  - completion-gate
