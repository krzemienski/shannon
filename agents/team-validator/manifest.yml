# Shannon agent manifest — team-validator
# Build step (`build/embed-skills.py`) reads this file and copies each
# embedded_skills entry from ../../skills/<name>/ into _built/skills/<name>/

name: team-validator
description: "Pillar 3: multi-perspective validator panel — 4 reviewers + aggregator"
priority: P0

embedded_skills:
  - judge
  - functional-validation
  - consensus-engine
  - visual-inspection
  - full-functional-audit

related_standalone_skills:
  - evidence-indexing
  - completion-gate
